# UI / UX Task Prompt

Use this when asking Claude to build, redesign, or polish a UI surface.

---

## Goal

(One sentence — what should the user be able to see/do after this work?)

## Acceptance Criteria

- [ ] (specific behavior, e.g. "Empty state shows X and CTA Y")
- [ ] (responsive: works at 360px, 768px, 1280px)
- [ ] (accessible: keyboard nav, ARIA labels, color contrast WCAG AA)
- [ ] (loading / error states defined)
- [ ] (matches existing components in `src/components/ui/`)

## Touchpoints

- Page/route: `(path)`
- New components: `(list)`
- Reused components: `(list)`
- Affected schemas / queries: `(list)`

## Out of Scope

- (what we are NOT doing this round)

## References

- Design mockup: (link / file)
- Inspiration: (link)
- Existing patterns to match: `src/components/...`
