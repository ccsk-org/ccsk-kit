# Original prompt (verbatim)

> Add a `/health` endpoint that returns `{ status: "ok", uptime }` and is covered by a test.

<!-- This file captures the user's request verbatim — never rewritten (memory-protocol). -->
<!-- This is a shipped EXAMPLE plan demonstrating the .ccsk/plans/ structure. -->
